#include "r_shapes.h"

#include "logger.h"

#include "v_funcs.h"

void R_DrawLine(window_t* window, float thickness, vertex2d_t point1, vertex2d_t point2)
{
    window = window;
    thickness = thickness;
    point1 = point1;
    point2 = point2;    // so theres no warnings when compiling
}